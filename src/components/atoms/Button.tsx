import styled from 'react-emotion'

export const Button = styled('button')({
  border: 0,
  borderRadius: '.2rem',
  cursor: 'pointer',
  fontSize: '.9rem',
  margin: '.25rem 0',
  outline: 0,
  padding: '.5rem 2rem',
  lineHeight: '1.45rem',
  width: '100%',
})
