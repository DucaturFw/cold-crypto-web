import styled from 'react-emotion'

export default styled('button')({
  border: 0,
  borderRadius: '.2rem',
  cursor: 'pointer',
  fontSize: '.9rem',
  margin: '.25rem 0',
  outline: 0,
  padding: '.8rem 2rem',
  width: '100%',
})
